/**
 * 1-- 
 *When a user types a domain like example.com into the browser, the browser first needs to find the IP address associated with that domain. It sends a request to a DNS resolver (usually provided by the ISP or a public DNS like Google or Cloudflare). The DNS resolver returns the public IP address mapped to that domain. That IP address may point directly to an EC2 instance, to an Elastic IP attached to it, or sometimes to a Load Balancer in front of it. DNS itself does not “store the IP inside the browser”; rather, the IP is stored in DNS records such as an A record or CNAME record.

Once the browser receives the IP address, it sends an HTTP or HTTPS request to that public IP. When the request reaches AWS, it first encounters the Security Group associated with the EC2 instance. A Security Group acts like a firewall and controls which ports are allowed to receive traffic. For example, port 22 may be open for SSH, port 80 for HTTP, and port 443 for HTTPS. If port 443 is allowed, HTTPS traffic is permitted to enter the instance. The Security Group does not decide request types; it simply allows or blocks traffic based on port rules.

After passing through the Security Group, the request reaches the EC2 instance. If a web server is listening on the allowed port, it receives the request. Typically, Nginx is running on the instance as a reverse proxy. A reverse proxy is a server that sits in front of application servers and provides a centralized place for configuration, routing, redirection, SSL handling, and sometimes caching or load balancing. Nginx usually listens on port 443 for HTTPS traffic.

When the request hits Nginx, it forwards (proxies) the request to the actual application server running on localhost:3000. This means that while the browser sends the request to the public domain and IP address, internally Nginx passes that request to the application running on port 3000 within the same machine. The browser never directly communicates with localhost; only Nginx does internally. So the complete flow becomes: Browser → DNS → Public IP (EC2) → Security Group → Nginx → localhost:3000.
 * 
 */