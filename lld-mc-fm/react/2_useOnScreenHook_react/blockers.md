1. create a grid of elements where the elements visible in the viewport needs to be tracked
2. track the elements visible in the viewport using intersection observer
3. give syntax of intersection observer
4. track the element using scroll event and getBoudingClientRect() - not that effective though