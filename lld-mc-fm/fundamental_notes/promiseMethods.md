1. Promise.all() waits for all promises and fails fast on the first rejection. Promise.allSettled() waits for every promise and reports both successes and failures. Promise.race() settles as soon as the first promise settles, whether resolved or rejected. Promise.any() returns the first fulfilled promise and only rejects if every promise fails. These methods are useful for coordinating concurrent asynchronous operations with different requirements.