1. main thing is to find a common way to compare the colors which we can get from window.getComputedStyle(element).color
2. document.body.appendChild(divElement) , i forgot the .body part
3. window.getComputedStyle(element).color