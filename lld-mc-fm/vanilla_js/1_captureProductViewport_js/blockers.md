Problem : Create block div elements and check if element is in viewport

1. add css to html
2. create dynamic divs on the page and put it into clean layout using grid
3. check if the element is in viewport 

Problem : Call a function when user stops scrolling or any other interactions for sometime.

1. what is debouncing and why we can use it trigger function during scroll
2. write a debounce function
